// Satu-satunya sumber konfigurasi API — file lain SELALU import dari sini,
// jangan pernah menulis ulang URL ini di file lain.
export const CONFIG = {
  // Ganti dengan URL deployment Web App GAS kamu (Deploy > New deployment > Web app)
  API_URL: 'https://script.google.com/macros/s/GANTI_DENGAN_DEPLOYMENT_ID/exec',
  SESSION_STORAGE_KEY: 'hafalan_session',
};
